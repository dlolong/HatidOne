# Copy to apps/web/.env.local, or run npm run demo:env for the local backend.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:55321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3003
# Explicit only. The seeded LOCAL database also enables demo payment flags.
HATIDONE_DEMO_MODE=false
# No service-role, maps, payment, SMS, push or AI keys are required.
