# Use only the public URL and anon/publishable key. Never use a service-role key.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Use your computer's LAN IP for both URLs when opening on a physical device.
EXPO_PUBLIC_WEB_URL=http://localhost:3000
