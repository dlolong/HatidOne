import { createClient } from '@/lib/supabase/server';
import { dateTime } from '@/lib/operations/data';
type ReferredRide={id:string;status:string;scheduled_at:string|null;pickup_address:string;dropoff_address:string;passenger_count:number;external_reference:string|null};
export async function PartnerReferrals({id}:{id:string}){const client=await createClient();const {data,error}=await client.rpc('get_partner_referrals',{p_organization_id:id});if(error)throw new Error('Partner referrals could not be loaded.');const rides:ReferredRide[]=Array.isArray(data)?data:[];return <section className="dashboard-card dashboard-wide"><h2>Referred guest bookings</h2><p>{rides.length} referred requests · {rides.filter(ride=>ride.status==='trip_completed').length} completed</p>{rides.length?<div className="table-scroll"><table><thead><tr><th>Route</th><th>Schedule</th><th>Status</th><th>Reference</th></tr></thead><tbody>{rides.map(ride=><tr key={ride.id}><td data-label="Route">{ride.pickup_address}<br/>{ride.dropoff_address}</td><td data-label="Schedule">{dateTime(ride.scheduled_at)}</td><td data-label="Status">{ride.status.replaceAll('_',' ')}</td><td data-label="Reference">{ride.external_reference??'—'}</td></tr>)}</tbody></table></div>:<p>No guest has booked using this partner’s link yet.</p>}</section>;}
