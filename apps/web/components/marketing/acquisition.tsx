import Link from 'next/link';
export function AcquisitionPage({ eyebrow, title, description, points, cta, href = '/organizations' }: { eyebrow:string; title:string; description:string; points:string[]; cta:string; href?:string }) {
  return <main className="landing-page"><nav className="landing-nav"><Link className="brand" href="/">HatidOne</Link><Link className="button button-secondary" href="/login">Sign in</Link></nav><section className="hero"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{description}</p><Link className="button button-primary" href={href}>{cta}</Link></section><section className="feature-grid">{points.map((point,index) => <article className="feature-card" key={point}><span>{String(index+1).padStart(2,'0')}</span><h2>{point}</h2></article>)}</section><p>HatidOne is preparing a verified mobility network. Ride availability depends on approved drivers and confirmed schedules.</p><nav className="button-row"><Link href="/drivers">Drivers</Link><Link href="/fleets">Fleets</Link><Link href="/partners/resorts">Resort partners</Link><Link href="/business">Corporate</Link><Link href="/routes/airport-to-makati">Airport transfers</Link></nav></main>;
}
