# Owner actions, in dependency order

Repository implementation does not authorize remote changes, production seed data, public deployment, paid accounts, signing material generation or transporting passengers. The following are pending actions, not requests to approve unfinished code. Consult RELEASE_REPORT.md for blockers before taking them.

1. **Choose and authorize an isolated staging backend.** Identify existing Supabase project/host, confirm its data classification and paid-resource implications, and authorize the exact deployment/migration action separately. Expected result: a known project/origin and administrative owner; no project credentials in reports. Do not add testers as Supabase organization/project administrators.
2. **Reconcile migration history and rehearse recovery.** Read hosted migration history and compare tracked files. Resolve historical duplicate `0004` version mapping without changing applied files. Never deploy `.local-backend` renumbered copies. Freeze new booking/dispatch on the old release, safely drain existing assigned/active trips, and run the zero-row preflight SQL in DEPLOYMENT.md; migration 0009 aborts if any remain. Run `npm run test:db` locally and follow DEPLOYMENT.md restore rehearsal on an authorized disposable target. Expected result: reviewed migration sequence, tested restore record and backward-compatible rollout plan. This cannot safely be replaced by a generic `db push` command.
3. **Define data/support/operations policy.** Supply privacy notice owner and review, retention/deletion procedure, support contact/hours, incident escalation, service geography, quote/toll rules, cancellation/no-show handling and reconfirmation cutoff. Retain 0% driver commission unless the approved policy is explicitly changed. Expected result: approved copy and implemented/tested account/privacy flow. The new Account requests page/RPC records a deletion request and operations can view it, but no deletion processor exists; completion stays blocked until retention and authorized Auth processing are approved.
4. **Configure staging without privileged client values.** Copy tracked examples to their ignored app env paths or use host build environment settings. Names: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SITE_URL`, `HATIDONE_ENVIRONMENT=staging`, `HATIDONE_DEMO_MODE=false`; each mobile app: `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY`, `EXPO_PUBLIC_APP_ENVIRONMENT=staging`. Use public HTTPS, then `npm run check:env:release`. Expected result: all environment checks PASS. It does not certify that the host contains no production data.
5. **Prepare and authorize web/API rollout.** On Node 24: `npm ci`, `npm run verify:code`, `npm run build:release`. Provision no new paid host automatically. Apply the reviewed migrations only after step 2 authorization and deploy using the chosen host's approved process. Expected result: known verified HTTPS origin, admin liveness/readiness checks, bookings initially paused, demo events rejected. No hosted URL currently exists in this handoff as verified evidence.
6. **Configure and test authentication.** In the selected Supabase project's Auth settings, disable public self-signup for the invited beta, invite individual tester accounts, and verify that direct public signup is rejected. UI signup hiding alone is not this control. Set Site URL to the real staging web origin and narrowly allow its callback plus the native scheme redirects from each app config. Web password recovery now exists: Login → Forgot your password → `/forgot-password`, verified `/auth/recovery/callback`, and `/reset-password`. Add the exact intended HTTPS recovery callback to the Auth redirect allowlist and verify the email template preserves it. Test signup confirmation and reset delivery with individual fictional accounts. Open the reset email in the same browser within 15 minutes, update the password, verify old-password rejection and link replay rejection, and check that an unrelated signed-in account is unchanged. No reset email delivery has been tested by the agent; this is a delivery/integration gate. If delivery is unavailable, use individual non-production invite/test accounts managed through supported Auth tooling; document that limitation without disabling production verification. Expected result: each app signs in/reopens/logs out without sharing sessions or caches.
7. **Prepare installable builds with authorized signing.** Follow docs/MOBILE_SIMULATOR_GUIDE.md, run `npm run mobile:preview:prepare`, and use app-local preview build commands or existing EAS profiles. Android SDK/JDK and owner signing are prerequisites for a distributed signed APK; iOS simulator and physical-device signing/distribution are distinct. Do not generate/upload release keys automatically. Expected result: actual artifact paths, version/backend/commit manifest, with installation evidence; configuration and exports alone do not meet this action.
8. **Run the full no-laptop staging test.** Install Passenger and Driver, stop Metro/local API, close/reopen both, and operate Admin on deployed web. Run the exact lifecycle and timeout/account-switch cases from QA.md. Record anonymized shared ride ID, versions, outcomes and artifact IDs, never tokens/PINs/real locations. `npm run verify:rc1` deliberately returns nonzero while its external gates lack execution evidence. Expected result: independent evidence suitable for an installable-beta verdict.
9. **Approve any real operation and distribution separately.** Complete PILOT_GATES.md legal/insurance/geography/fare/staffing gates before compensated transport. Review actual current store requirements when the distribution choice is known; prepare truthful metadata/privacy links and individual private reviewer credentials, then authorize submission separately. Expected result: evidence for the specific gate, never a blanket production-ready assertion.


Local-only fixture credential transition: no shared fixed password is supported. New local initialization prepares distinct account passwords in ignored `.local-backend/test-accounts.json` and passes them only through a private mode-0600 staged seed. Ordinary start preserves the file, while status/stop never generate or rotate credentials. Existing local database accounts were not changed by this implementation. If their credentials no longer match a prepared file, preserve valuable local data; only on a disposable backend explicitly choose `npm run demo:reset -- --confirm-local-reset`. This deletes local records and installs newly rotated individual credentials. A failed reset retains the previous private account file for recovery. No fixture credentials were generated or account changes executed by the agent.
